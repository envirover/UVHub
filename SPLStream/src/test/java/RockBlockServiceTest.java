import java.io.IOException;
import java.text.ParseException;
//import java.util.Date;

import org.junit.Test;

//import com.MAVLink.MAVLinkPacket;
//import com.MAVLink.common.msg_high_latency;
//import com.envirover.spl.stream.DynamoDBStream;

public class RockBlockServiceTest {

    @Test
    public void testPostMobileOriginatedMessage() throws ParseException, IOException {

        //DynamoDBStream stream = new DynamoDBStream();

        //stream.writePacket("012345678901234", new Date(), getSamplePacket());
    }

    /*
    private MAVLinkPacket getSamplePacket() {
        msg_high_latency msg = new msg_high_latency();
        msg.latitude = 523867;
        msg.longitude = 2938;
        msg.sysid = 1;
        msg.compid = 2;
        return msg.pack();
    }
    */

}
